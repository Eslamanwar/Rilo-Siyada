{
  "name": "siyada-server",
  "version": "0.1.0",
  "type": "module",
  "description": "Siyada — UAE AI Data Shield backend",
  "scripts": {
    "start": "node --env-file=.env src/index.js",
    "dev":   "node --env-file=.env --watch src/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
